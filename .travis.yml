sudo: required
services:
  - docker

before_install:
  - docker build -t jcsolutions/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true jcsolutions/react-test npm test -- --coverage

after_success:
  - docker build -t jcsolutions/multi-client ./client
  - docker build -t jcsolutions/multi-nginx ./nginx
  - docker build -t jcsolutions/multi-server ./server
  - docker build -t jcsolutions/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push jcsolutions/multi-client
  - docker push jcsolutions/multi-nginx
  - docker push jcsolutions/multi-server
  - docker push jcsolutions/multi-worker
